{
  "name": "@licham/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "verify": "vitest run --reporter=verbose",
    "typecheck": "tsc --noEmit",
    "check:compare": "node --experimental-strip-types --experimental-loader ./scripts/ts-extension-loader.mjs scripts/compare.ts",
    "verify:ltp": "node --experimental-strip-types --experimental-loader ./scripts/ts-extension-loader.mjs scripts/verify-ltp.ts"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
